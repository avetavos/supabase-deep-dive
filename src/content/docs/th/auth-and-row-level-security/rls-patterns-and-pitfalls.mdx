---
title: 'RLS Patterns and Pitfalls'
description: 'Pattern ของ row level security ที่เจอบ่อยในโลกจริง บวกกับ pitfall เรื่อง per-operation และ performance ที่มือใหม่ Supabase มักเจอ'
sidebar:
  order: 4
---

import Mermaid from '../../../../components/Mermaid.astro';
import Callout from '../../../../components/Callout.astro';
import Quiz from '../../../../components/Quiz.tsx';
import ProgressTracker from '../../../../components/ProgressTracker.tsx';

## แนวคิดในหนึ่งประโยค

RLS ในโลกจริงส่วนใหญ่คือการผสม pattern ไม่กี่แบบเข้าด้วยกัน — owner-only, public-read-owner-write, role-based — และ bug ของ RLS ในโลกจริงส่วนใหญ่ก็มาจากการลืม policy ของ operation ใด operation หนึ่ง หรือลืม index column ที่ policy ใช้กรอง

## Owner-only rows

pattern จากบทที่แล้ว row จะมองเห็นหรือแก้ได้เฉพาะ user ที่เป็นเจ้าของเท่านั้น โดยใช้ `auth.uid() = user_id` ทั้งใน `using` และ `with check`

```sql
create policy "Owners can update their own rows"
on public.todos
for update
using (auth.uid() = user_id)
with check (auth.uid() = user_id);
```

## Public read, owner-only write

รูปแบบที่พบบ่อยมากสำหรับ content แบบ blog post หรือ comment ใครก็อ่านได้ แต่แก้หรือลบได้เฉพาะเจ้าของ row เท่านั้น แบบนี้ต้องใช้ **policy สองอันแยกกัน** อันละทิศทาง เพราะ `using (true)` ของ `select` ไม่ได้บอกอะไรเลยว่าใครเขียนได้บ้าง

```sql
-- Anyone (including anon) can read
create policy "Anyone can read posts"
on public.posts
for select
using (true);

-- Only the owner can update their own post
create policy "Owners can update their posts"
on public.posts
for update
using (auth.uid() = user_id)
with check (auth.uid() = user_id);
```

## Role-based access

บางครั้งสิทธิ์การเข้าถึงขึ้นกับ role ไม่ใช่ความเป็นเจ้าของ เช่น admin ที่ moderate content ของทุกคนได้ วิธีที่นิยมคือใช้ table `user_roles` แล้ว join เข้าไปใน policy condition (custom claim ที่ฝังตรงใน JWT ก็เป็นอีกทางเลือกหนึ่ง แต่ table แบบ join จัดการง่ายกว่าเพราะไม่ต้องออก token ใหม่ทุกครั้ง)

```sql
create policy "Admins can delete any post"
on public.posts
for delete
using (
  exists (
    select 1 from public.user_roles
    where user_roles.user_id = auth.uid()
    and user_roles.role = 'admin'
  )
);
```

## Pitfall: policy อันเดียวไม่ครอบคลุมทุก operation

RLS ถูกประเมิน **แยกตาม operation** — `select`, `insert`, `update`, `delete` แต่ละอันเช็คกับ policy ของตัวเองแยกกันโดยอิสระ table หนึ่งใบจึงมี policy `select` ที่ใช้งานได้ปกติ แต่ไม่มี policy `insert` เลยก็ได้ง่าย ๆ ผลคือ อ่านได้ปกติ แต่ทุกการเขียนจะ fail แบบเงียบ ๆ ด้วย permission error ซึ่งดูเหมือน bug ของโค้ดฝั่งแอป ทั้งที่จริง ๆ แค่ลืม policy ก่อน deploy table ควรเช็ก operation ที่แอปต้องใช้กับ table นั้นทีละอัน แล้วให้แน่ใจว่ามี policy ครบทุกตัว

## Pitfall เรื่อง performance: column ที่ policy ใช้กรองแต่ไม่มี index

condition ของ policy รันกับทุก row ที่ Postgres พิจารณาใน query เหมือนกับ `where` clause เป๊ะ ถ้า column ที่ policy ใช้กรอง — ส่วนใหญ่คือ `user_id` ในตัวอย่างข้างบน — ไม่มี index query ที่ดูเรียบง่ายอาจกลายเป็น full sequential scan ทันทีที่ table มีข้อมูลจริงเยอะขึ้น ให้ index column ที่ RLS policy อ้างอิงเหมือนที่ทำกับ column ที่ถูก filter บ่อย ๆ ทั่วไป

```sql
create index on public.posts (user_id);
```

อีกจุดที่ควรรู้ไว้คือการห่อ `auth.uid()` ด้วย `select` ภายใน policy condition จะทำให้ Postgres ประเมินมันแค่ครั้งเดียวต่อ query แทนที่จะประเมินทุก row ซึ่งมีผลชัดเจนเมื่อ table มี row เยอะขึ้น

```sql
create policy "Owners can update their posts"
on public.posts
for update
using ((select auth.uid()) = user_id);
```

## Test policy ก่อน ship

อย่าคิดเอาเองว่า policy ทำงานถูกต้อง ให้ test ในฐานะ user ที่มันตั้งใจจะจำกัดสิทธิ์ table editor ของ Supabase Studio มี feature impersonation ไว้สำหรับเรื่องนี้โดยเฉพาะ หรือจะทำแบบเดียวกันตรง ๆ ใน SQL session ก็ได้ด้วยการสลับ role แล้วตั้ง claim ของ JWT ให้เหมือน request จริง

```sql
set role authenticated;
set request.jwt.claims = '{"sub": "11111111-1111-1111-1111-111111111111", "role": "authenticated"}';

select * from public.posts; -- now runs exactly as that user would see it

reset role;
```

<Mermaid title="A missing policy silently blocks one operation while others keep working" code={rlsPitfallsDiagram} />

export const rlsPitfallsDiagram = `flowchart TB
  subgraph missing["Table with only a SELECT policy"]
    m1["select policy: present"] --> m2["Reads work"]
    m3["insert policy: missing"] --> m4["Writes silently blocked"]
  end
  subgraph covered["Table with all four policies"]
    c1["select / insert / update / delete: all present"] --> c2["Reads and writes both work as intended"]
  end`;

<Callout title="สรุปหนึ่งบรรทัด">
RLS ในโลกจริงประกอบจาก pattern ไม่กี่แบบ — **owner-only**, **public-read-owner-write** (ที่ต้องใช้ policy สองอันแยกกัน หนึ่งสำหรับ select อีกหนึ่งสำหรับ operation ฝั่งเขียน) และ **role-based access** ผ่าน table `user_roles` ที่ join เข้ามา หรือผ่าน custom JWT claim แต่ความผิดพลาดสองแบบเดิมคือต้นเหตุของ bug ส่วนใหญ่ — ลืม policy ของ **operation ใด operation หนึ่ง** ทั้งที่ operation อื่นยังทำงานปกติ (อ่านได้ แต่เขียนแล้ว fail แบบเงียบ ๆ) และการอ้างอิง **column ที่ไม่มี index** ใน policy condition ซึ่งทำให้ query ที่ดูไม่มีพิษภัยกลายเป็น sequential scan เมื่อข้อมูลเยอะขึ้น ให้ test policy ด้วยการ impersonate เป็น user ที่มันจะจำกัดสิทธิ์เสมอก่อน ship
</Callout>

export const quizRlsPatterns = [
  {
    q: 'ทำไม public read, owner-only write ถึงต้องใช้ policy สองอันแยกกัน แทนที่จะใช้อันเดียว',
    options: [
      'Postgres บังคับให้ต้องมีอย่างน้อยสอง policy ต่อ table เสมอ',
      'policy select คุมแค่ว่าใครอ่านได้ ไม่ได้บอกอะไรเลยว่าใคร insert, update หรือ delete ได้ จึงต้องมี policy แยกสำหรับฝั่งเขียน',
      'ไม่จำเป็น policy อันเดียวก็พอเสมอ',
      'with check ใช้ร่วมกับ using ใน policy เดียวกันไม่ได้เลย',
    ],
    answer: 1,
  },
  {
    q: 'pitfall เฉพาะของการลืม policy สำหรับ operation หนึ่งบน table คืออะไร',
    options: [
      'ทั้ง table จะเปิดให้ทุกคนอ่านได้',
      'operation นั้นถูกบล็อกแบบเงียบ ๆ (เช่นเขียนไม่ได้) ทั้งที่ operation อื่นที่มี policy ของตัวเองยังทำงานปกติ เพราะ RLS ประเมินแยกตาม operation',
      'Postgres จะไม่ยอม start เลย',
      'policy ที่มีอยู่ทั้งหมดบน table จะถูกปิดใช้งานอัตโนมัติ',
    ],
    answer: 1,
  },
  {
    q: 'ทำไม policy condition ที่อ้างอิง column ไม่มี index ถึงทำร้าย performance เมื่อข้อมูลเยอะขึ้น',
    options: [
      'RLS policy ไม่เกี่ยวกับ index เลย',
      'condition ของ policy ถูกประเมินต่อทุก row ที่ query พิจารณา ถ้าไม่มี index Postgres อาจต้องทำ full sequential scan',
      'column ที่ไม่มี index จะทำให้ policy ถูกข้ามไปทั้งหมด',
      'มันกระทบแค่ operation insert เท่านั้น ไม่กระทบ select',
    ],
    answer: 1,
  },
  {
    q: 'วิธีที่ใช้งานได้จริงในการตรวจสอบว่า RLS policy ทำงานตามที่ตั้งใจก่อน ship คืออะไร',
    options: [
      'อ่าน SQL แล้วสมมติว่ามันถูกต้อง',
      'impersonate เป็น target user ผ่าน Studio หรือ set role/request.jwt.claims ใน SQL session แล้วรัน query ในฐานะ user คนนั้น',
      'ปิด RLS ชั่วคราวเพื่อดูผลลัพธ์',
      'ไม่มีทาง test RLS ได้ก่อนขึ้น production',
    ],
    answer: 1,
  },
];

<Quiz client:visible id="auth-and-row-level-security/rls-patterns-and-pitfalls" questions={quizRlsPatterns} />

<ProgressTracker client:visible id="auth-and-row-level-security/rls-patterns-and-pitfalls" />
